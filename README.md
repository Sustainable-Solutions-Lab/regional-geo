# regional-geo

Analysis of WRF regional climate model simulations.

## Setup

```bash
pip install -r requirements.txt
```

## Data

WRF post-processed output files (NetCDF format) should be placed in `data/input/`.
